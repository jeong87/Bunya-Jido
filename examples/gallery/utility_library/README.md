# Clear Text Utilities

This miniature exports `slugify`, `wrap_label`, and their `FormatOptions`.
Callers may use the functions independently; the examples show usage but do
not define a runtime sequence.

Its most honest map is an API reading route with `none_with_reason` scenario
policy.
